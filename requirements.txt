# Pinned versions this pipeline was built and validated with (Python 3.12).
# torch: install the build matching your hardware BEFORE the rest —
#   GPU (NVIDIA):  pip install torch --index-url https://download.pytorch.org/whl/cu128
#   CPU only:      pip install torch
pandas==3.0.5
numpy==2.5.2
pyarrow==25.0.1
scipy==1.18.0
scikit-learn==1.9.0
statsmodels==0.14.6
sentence-transformers==5.7.0
requests==2.34.2
yfinance==1.6.0
tqdm==4.70.0
pypdf==6.16.1
pytest
